let v = 'Tuesday';

console.log( v => {
    if (v == 'Wednesday') {
        return true;
    }
    else {
        return false;
    }
})

const city = "Seoul";

function logCitySkyline() {
    let skyscraper = "EmpireStateBuilding";
}
// 4: Yes, since city is declared in global scope
// 5: No, since skyscraper is defined inside the scope of logCitySkyline and accesible only inside it